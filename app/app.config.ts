// Nuxt UI design tokens. `primary` drives the green accent that nods to Nuxt;
// `neutral` sets the grayscale used across surfaces, borders, and text.
// Swap these to rebrand the whole UI in one place.
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'slate'
    }
  }
})
