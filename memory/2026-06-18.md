# 2026-06-18

## Staging deploy setup

Got the repo wired for staging deploy via GitHub Actions (push-to-main → `deploy-staging.yml`).

**Already provisioned on Cloudflare (account `jamie@island-web.ca` / `619f1f85e2ab095025ecf7b40c6939e5`):**
- D1 `stems-staging` (`742c0b4f-95f8-4ca0-abfe-408deef86e85`) — remote had 0 tables, migrations apply on first CI deploy
- R2 `stems-files-staging`
- Workers.dev subdomain: `jamiecurnow` → staging URL = `https://stems-staging.jamiecurnow.workers.dev`

**Done this session:**
- gh repo secrets set: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN` (token validated against D1)
- `DEPLOY_ENABLED=true` repo var + `staging` environment already existed
- wrangler.jsonc staging: `PUBLIC_BASE_URL` → real workers.dev URL; `MAIL_FROM` → `Stems <hello@island-web.ca>` (verified Resend domain; stems.market not owned yet)
- deploy-staging.yml environment url → real URL
- `npm run build` passes locally

**Decisions:** deploy via push (not manual `wrangler deploy`); Resend included; Stripe deferred.

**Remaining (needs first deploy to create the worker first):**
1. User commits + pushes working tree to main → CI deploys, creates `stems-staging` worker
2. Then set runtime worker secrets via `wrangler secret put --env staging`:
   - `BETTER_AUTH_SECRET` (generate), `ADMIN_API_SECRET` (generate)
   - `RESEND_API_KEY` (have it), `BETTER_AUTH_URL` = `https://stems-staging.jamiecurnow.workers.dev`
3. Stripe secrets deferred — Stripe plugin only attaches when `STRIPE_SECRET_KEY` set, so magic-link auth fine without it.

Note: worker secrets can't be set until the worker exists (wrangler errors "run wrangler deploy first"). CI does NOT manage these — they're a one-time manual `wrangler secret put`.

## ✅ STAGING IS LIVE (completed same night)

URL: **https://stems-staging.jamiecurnow.workers.dev**

What happened:
- First CI run failed: `npm ci` rejected the lockfile (authored by local npm 11, CI runs node22/npm10). Fixed by regenerating `package-lock.json` with `npx npm@10 install --package-lock-only` and pushing. Commit `267e4b5`.
- CI deploy then succeeded (build → D1 migrations applied to remote → worker deployed).
- Set 4 runtime secrets via `wrangler secret put --env staging`: `BETTER_AUTH_SECRET`, `ADMIN_API_SECRET` (both generated with `openssl rand -hex 32`), `RESEND_API_KEY`, `BETTER_AUTH_URL`.
- Verified live: `/` → 302 → `/discover` (200); `/api/search?q=rose` → 200 (remote D1 connected); `/api/profile/me` → 401 (auth works, not 500); magic-link POST → `{"status":true}`; Resend domain `island-web.ca` = verified.

**Gotcha for future / production deploy:** keep CI lockfile npm-10-compatible OR bump CI `node-version` to 24 (npm 11) to match local. Same fix will be needed for the production workflow.

Stripe still deferred (plugin only attaches when `STRIPE_SECRET_KEY` set). Production env not yet deployed — same secret-setting dance will be needed there.
