# 2026-06-30

## Blog redesign (design_handoff_blog)

Implemented the blog redesign handoff. Three files changed:

- **`content.config.ts`** — added optional `image` + `imageAlt` fields to the `blog` collection (separate from share-only `ogImage`).
- **`app/pages/blog/index.vue`** — rebuilt: floral-wash page header (mirrors how-it-works) → lead post (`posts[0]`, full-width optional feature image, big title) → hairline list rows (`posts.slice(1)`). Desktop list rows are two-column flex with the thumbnail on the right (`lg:order-last`) so text left-edges align and image-less rows read full width; mobile stacks image-on-top. Lead + rows get `-mx-6 hover:bg-peach-50` wash. `max-w-[1040px]` editorial measure.
- **`app/pages/blog/[slug].vue`** — `max-w-[880px]` container, header + body in centred `max-w-[720px]` column, optional hero image breaks out to full 880px (440px desktop + media shadow / 230px mobile). Applied the prose spec via arbitrary `.prose` descendant selectors (body ink `#4A453E`, `#C9A99E` markers, soft-bordered peach links, EB Garamond h2).

### Notes
- **`@nuxt/image` is NOT installed** — README suggested `<NuxtImg>` but explicitly allowed `<img>`. Used plain `<img loading="lazy">`. If we want the image pipeline (roadmap/06), install `@nuxt/image` and swap `<img>` → `<NuxtImg>` in both pages.
- Draft pill uses the spec's deeper peach (`bg-[#FCE3DD] text-[#C0503F]`), not `bg-primary/10` like before.
- Left the existing real post image-less (no photo asset shipped; it's a draft) — demonstrates the optional-image fallback.
- Pre-existing typecheck error at `nuxt.config.ts:81` (unused `@ts-expect-error`) — not mine.
- Didn't run `npm run format` (the gotcha); prettier'd only the two edited files.
