# 2026-06-26

## Built the whole marketing system (overnight, autonomous)

Jamie asked (before bed) to replicate the marketing/SEO setup from the LAND
(Edgelands) sibling repo onto Stems, in Stems' own tone, plus draft the About
page and first blog post, and leave a clear pick-up plan. Worked autonomously.

### What I made
- **`marketing/`** folder, modelled on LAND's structure but rebuilt for Stems'
  opposite posture (warm/honest/relationship-driven vs Edgelands' cash-grab):
  - `00-foundations/`: `positioning.md` (the strategic heart: Stems = the
    shopfront/Linktree for small growers; grower is the hero we acquire; buyers
    are demand the grower brings), `brand-voice.md` (master), `social-voice.md`
    (Instagram), `bio-and-handles.md`.
  - Channel folders (README + todo each): `01-instagram`, `02-grower-outreach`
    (THE priority channel: FFTF, markets, county groups, slow flowers),
    `03-seo-content` (+ `pillar-articles.md` 8-post backlog + `prompt-blog-article.md`),
    `04-word-of-mouth`, `05-email`, `06-community-press`, `07-aeo-geo`.
  - `seo-roadmap/`: technical SEO gap analysis + 4 dispatchable dev briefs
    (sitemap/robots, global schema, @nuxt/content blog system, marketing home).
  - `STATUS.md` (the pick-up desk + validation checklist) and `metrics.md`
    (activated growers, not vanity). **STATUS.md is the entry point.**
- **`APP_INFO.md`**: filled in (was an empty template).
- **`app/pages/about.vue`**: rewrote the "coming soon" placeholder into real copy.
- **`content/blog/how-to-sell-flowers-locally-without-a-website.md`**: first blog
  post, `draft: true`, has FAQ schema. Can't go live until the content system
  (`seo-roadmap/03`) is built.
- **Bug fix**: `stems.app` -> `stems.market` in `discover.vue` + `onboarding.vue`
  (every new grower saw the wrong domain). Flagged for Jamie to confirm the
  canonical domain.

### Decisions / notes
- Used two background agents for the channel folders and the SEO briefs; wrote
  the voice, positioning, About page, blog post, README and STATUS myself.
- User-facing copy (About, blog post, APP_INFO) is em-dash-clean. Internal
  planning/dev docs still contain em-dashes (acceptable; the rule is user-facing).
- Production domain is **stems.market** (per wrangler + contact emails).

### Waiting on Jamie (see STATUS.md validation checklist)
Confirm domain; read+approve voice docs and About copy; fact-check blog post;
green-light which SEO tech to implement and by whom; write founder bio; verify
FFTF numbers + British Flowers Week dates; claim handles.

### Nothing committed (per git rule). All changes are in the working tree.
