# Dependencies
node_modules

# Logs
logs
*.log

# Local env files
.env
.env.local
.env.*.local

# Nuxt artifacts
.nuxt
.output
.nitro
.cache

# @nuxt/content local dev database (built from content/**)
.data

# Wrangler local state (D1, KV, DOs) and deploy artifacts
.wrangler

# Auto-generated Workers types (the `wrangler types` output — we use our own
# server/types/cloudflare.d.ts; including this file in the TS program breaks
# the browser tsconfig).
worker-configuration.d.ts

# Editor
.vscode
.idea
.DS_Store
.claude/settings.local.json
USER.md
