# 2026-06-19

## UX batch (11 tweaks, one pass)

Worked through a batch of changes from Jamie, pushed to main:

1. **Crop drag clamp** — `ImageCropModal` pan is now clamped so the framed image
   always covers the frame (no exposed edge); re-clamps on zoom-out.
2. **Unsaved-changes guard** on `/account/edit` — snapshot/`isDirty` diff →
   `onBeforeRouteLeave` confirm modal + `beforeunload`. Save/Discard set
   `allowLeave` to bypass.
3. **Multi-photo upload** — `ImageCropModal` gained a `multiple` prop + a file
   queue; `GalleryUploader` passes it, so growers pick several at once (each
   framed in turn). Overflow past `max` dropped cleanly.
4. **Flower form** "Status" label → "Availability status".
5. **Availability text** — new `availabilityText()` in `shared/utils/flowers.ts`.
   Public `/@handle` list + drawer now show "Availability: Good" / "…: 120 stems"
   as plain text (no badge/chip), reading identically in both places.
6. **Logged-out bottom nav** — replaced the small "Sign in" tab with a prominent
   primary **Start selling** CTA (aimed at converting passing flower sellers).
7. **Floating "Contact to buy"** on `/@handle` — fixed pill above the nav,
   appears once the inline CTA scrolls off (`useElementVisibility`), gap above nav.
8. **@ search** on discover — search endpoint strips a leading `@` so "@handle"
   matches the bare stored handle.
9. **Per-flower "Updated X ago"** on the list rows + drawer — new pure
   `timeAgo()` in `shared/utils/time.ts` (SSR-safe, minutes-granularity).

typecheck + full `nuxt build` both pass. New shared util: `shared/utils/time.ts`.

## Flower availability status feature

Added a categorical `availabilityStatus` to flowers, alongside the existing
`stemsAvailable` count. Both optional/independent — grower can use either, both
or neither. Statuses: Good / Limited / Very limited / Sold out / Available
midweek / next week / soon (slugs: good, limited, very_limited, sold_out,
midweek, next_week, soon) — defined in `shared/utils/flowers.ts` with semantic
colours for badges.

Key decisions:
- Sold-out is now derived from EITHER signal (count `0` OR `sold_out` status) —
  `isSoldOut`/`isInStock` were changed to take a flower-like object. No external
  callers existed, so the signature change was safe.
- Public profile list: status / stems / price now **stack vertically** (didn't
  fit horizontally); status shows as a coloured `UBadge`. Stems line drops the
  word "available" → "120 stems". Falls back to "Available" only when there's no
  status and no count.
- Migration `0011_flower_availability_status.sql` (plain ADD COLUMN, nullable).

Committed `6448ac0`, pushed to main → triggers `deploy-staging.yml`. User wants
to test on staging (https://stems-staging.jamiecurnow.workers.dev).

typecheck + full `nuxt build` both pass locally.

## Legal/marketing pages + discover nav

Added the public legal/marketing pages a prod app needs. Legal entity is
**Guardline Ltd** (England & Wales, company no. 13323382); contact
`hello@stems.market` (prod MAIL_FROM). Modelled on edgelands' `/privacy`,
`/cookies`, `/terms` but rewritten Stems-specific.

New pages (all `layout: 'default'` — slim public chrome, shareable web pages):
- `/privacy` — UK GDPR. Stems specifics: magic-link auth (no passwords stored),
  public-by-design grower pages, and the no-in-app-messaging contact handoff
  (we don't see/send/store buyer↔grower chats). Subprocessors: Stripe,
  Cloudflare (D1/R2), Resend.
- `/cookies` — real cookie table (`better-auth.session_token`, `stems_consent`,
  `stems_ref`). "Change your preferences" reuses `useConsent()` +
  `<LayoutConsentManageDialog>`.
- `/policies` — small hub linking privacy + cookies (home for legal pages as
  more get added, e.g. terms).
- `/about`, `/blog` — branded "coming soon" placeholders.

Discover hero: added a quiet `About · Blog · Policies` nav between the "or sign
in" CTA and the sticky search, styled like the `Local · Seasonal · Grown`
eyebrow. Looks great (screenshotted).

## Removed Google Analytics (PostHog deferred until >1 user)

Jamie wants GA gone now, PostHog later. Decision (his call): **keep the cookie
consent banner + `useConsent` system dormant, ready for PostHog** — only remove
Google.

Deleted: `app/plugins/analytics.client.ts` (GTM bootstrap), `useAnalytics.ts`
(dataLayer), `useAnalyticsIdentity.ts`, `server/utils/analytics.ts` (GA4
Measurement Protocol). Removed the `useAnalyticsIdentity` call from `app.vue`.
Stripped the Google Consent Mode `gtag('consent',…)` pushes from `useConsent`
(now a plain cookie-backed preference store; PostHog should consult
`state.value.analytics`). Removed GA config: nuxt.config `runtimeConfig.public`,
`.env.example` GTM/GA4 block, `cloudflare.d.ts` env types, wrangler commented
examples. Updated privacy/cookies copy + COMPOSABLES/SERVER_UTILS/SERVER_ENDPOINTS
docs.

**For PostHog later:** conversion events used to fire via `sendServerEvent`.
Left `// TODO(posthog)` seams (matching the webhook's existing commented-hook
style) at the exact spots: `server/api/stripe/webhook.post.ts` → `purchase`
(invoice.paid/subscription_cycle, after `grantReferrerReward`), `trial_start`
(customer.subscription.created/trialing), `subscription_cancelled`,
`subscription_reactivated`; and `server/routes/r/[code].get.ts` →
`referral_landed` (hash the code first). Re-add the DB lookups for userId when
wiring those (I removed the now-unused lookups to avoid dead code).

typecheck + full `nuxt build` both pass.

## Em-dash cleanup + SHIPPED TO PROD

Jamie dislikes em-dashes in copy (reads as AI). Stripped `—`/`–` from all
user-facing copy in the new pages (used commas/colons/periods, not blind
hyphens). Jamie did a parallel repo-wide em-dash→`-` pass on emails
(`welcome.ts`, `_layout.ts`), `@[handle]`, account/edit, flowers, onboarding.
Added an always-on rule `.agent/rules/copywriting.md` + a harness memory
([[no-em-dashes-in-copy]]) so they stay out. Remaining em-dashes are only in
code comments / `.md` docs — intentionally left (rule is copy-only).

Committed everything as `2d54c06`, pushed to main (Jamie OK'd commit+push).
Staging auto-deployed; triggered **production** via `gh workflow run
deploy-production.yml` → both runs green (~2 min). Verified live on
https://stems.market: /discover /privacy /cookies /policies /about /blog all
200, and 0 googletagmanager/gtag refs in prod HTML. Dev server (:3002) killed.
